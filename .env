# Shared Docker environment — loaded automatically by Docker Compose.
# These are LOCAL DEVELOPMENT defaults only.
# For any real deployment, override all three in .env.local (gitignored) or via host environment variables.
POSTGRES_DB=symbiosis
POSTGRES_USER=symbiosis
POSTGRES_PASSWORD=symbiosis
